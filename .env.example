# Server
PORT=3000
NODE_ENV=development
DATABASE_URL="file:./data/m3u8preview.db"
JWT_SECRET="change-me-in-production"
JWT_REFRESH_SECRET="change-me-in-production-refresh"
PROXY_SECRET="change-me-proxy-secret-in-production"
CORS_ORIGIN=http://localhost:5173

# Client
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Docker (docker compose 使用，本地开发可忽略)
# DOCKER_PORT=80              # Nginx 对外端口
# CORS_ORIGIN=http://localhost # Docker 生产环境跨域来源

# Thumbnail
# THUMBNAIL_CONCURRENCY=5    # 封面生成并发数（1-20，默认 5）

# Source Plugins（动态解析源插件）
# SOURCE_PLUGIN_PREVIEW_CONCURRENCY=3   # 批量解析起始并发数（默认 3；命中风控会在单次批量内自动逐级降到 1）
